use DB_File;
use Fcntl;

tie (%myh, DB_File, 'sample.db', O_RDWR, 0740, $DB_HASH);

while (($key, $val) = each (%myh) ){
        #print "$key = ", length($val), "\n";
        print "$key = \n";
}
untie %myh;

